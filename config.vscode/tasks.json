{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "clang++",
            "args": [
                "-g",
                "-std=c++11",
                "${workspaceFolder}/*.cpp",
                "-o",
                "${workspaceFolder}/jps_pathfinding"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}